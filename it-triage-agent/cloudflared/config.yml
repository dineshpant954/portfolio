# Cloudflare named tunnel config template.
# Replace hostname with your own DNS record managed in Cloudflare.
ingress:
  - hostname: n8n.example.com
    service: http://n8n:5678
  - service: http_status:404
